# ---- 运行时数据（绝不提交）----
# 包含 SQLite 数据库、上传的 ACMI/资料文件、备份包
var/

# ---- 虚拟环境 ----
.venv/
venv/

# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# ---- 测试与覆盖率 ----
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# ---- 环境与密钥 ----
# ⚠️ 生产环境密钥通过环境变量注入，绝不入库
.env
.env.*
!.env.example

# ---- 私钥（纵深防御）----
# 部署密钥的正规位置是 /var/lib/gfvfw/.ssh/（**仓库之外**），
# 见 deploy/DEPLOY.md §2。这里再兜一层：万一有人把密钥生成到了仓库里，
# 也不要让 git 把它提交上去。
*.pem
id_rsa*
id_ed25519*
*_deploy
*_deploy.pub
.ssh/

# ---- 编辑器 / 系统 ----
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini

# ---- 临时产物 ----
*.log
*.tmp

# 参考源码（CamReader / SITREP 的解压产物，第三方代码，不入库）
reference/
*.zip

# ---- 逆向工程临时产物 ----
# LogbookEditor.exe 的反汇编/字符串转储（1.5 MB+）。**可随时重建**，
# 命令见 gfvfw/lbk_parser.py 的模块注释与 README「BMS Logbook 上传」一节：
#   G:\mingw64\bin\objdump.exe -d -M intel LogbookEditor.exe > _re/lbk/disasm.txt
_re/
