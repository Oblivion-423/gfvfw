# GFVFW-Manager 环境变量示例
#
# 复制成 .env 后按本机情况修改。所有变量都有 GFVFW_ 前缀。
# ⚠️ .env 已被 .gitignore 忽略（本文件不会被忽略，可安全提交）。

# ---- 必填：生产环境必须设置 ----
# 会话与 CSRF 签名密钥。不设置会使用默认值，任何人都能伪造会话。
# 生成方法：python -c "import secrets;print(secrets.token_urlsafe(48))"
# GFVFW_SECRET_KEY=请改成随机长串

# ---- 战役管理：Falcon BMS 安装目录 ----
# 解析 .cam 战役存档需要安装目录下的剧场数据表：
#   Data\TerrData\Objects\Falcon4_CT.xml        类表（决定 .uni 记录类型）
#   Data\TerrData\Objects\Falcon4_UCD.xml       单位数据
#   Data\TerrData\Objects\Falcon4_VCD.xml       载具/飞机数据
#   Data\TerrData\Objects\Falcon4_WCD.xml       武器数据（含 SAM 射程）
#   Data\Campaign\CampObjData.xml               目标点清单（约 6944 条）
#   Data\TerrData\Objects\ObjectiveRelatedData\OCD_*.XML  目标类型映射
# Korea 用 Data\；Add-On 剧场用 Data\Add-On <剧场>\
#
# 不设置这项，「战役管理」的上传会明确报错，其余功能不受影响。
GFVFW_BMS_INSTALL_PATH=G:\BMS\Falcon BMS 4.38

# ---- 其他（一般不用改）----
# GFVFW_DATABASE_URL=sqlite+pysqlite:///G:/GFVFW-Manager/var/gfvfw.sqlite3
# GFVFW_STORAGE_DIR=G:\GFVFW-Manager\var\storage
# GFVFW_MAX_CAM_BYTES=33554432
# ---- 可选：联队自备的剧场地图目录 ----
# 态势图的底图默认用 BMS 自带图，但那些图很大（Hellas 4K 48 MB、8K 192 MB、
# 16K 768 MB）。把自压的正方形 PNG 放这里会**优先**被选中，首次加载快很多。
# 注意：必须是**正方形**（边长等于 2 的幂最好，如 2048×2048），
# 因为战役网格是 0..1023 的正方形，非正方形图无法一格对一格铺满。
# GFVFW_BMS_MAP_DIR=G:\GFVFW-Manager\var\maps

# GFVFW_MAX_ACMI_BYTES=268435456
